[project]
name = "whisper-demo"
version = "0.1.0"
description = ""
authors = [
    {name = "satomi",email = "satomi@github.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp (>=3.11.14,<4.0.0)",
    "fastapi (>=0.115.12,<0.116.0)",
    "services (>=0.1.1,<0.2.0)",
    "api (>=0.0.7,<0.0.8)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
