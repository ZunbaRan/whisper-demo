[tool.poetry]
name = "web-crawl"
version = "0.1.0"
description = "Web 爬虫服务"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = ">=3.12.0,<3.13"
fastapi = "0.110.0"
uvicorn = "0.29.0"
crawl4ai = "^0.4.248"
aiofiles = "24.1.0"
beautifulsoup4 = "^4.12.0"
selenium = "4.30.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = "src.crawler.api:start_app" 