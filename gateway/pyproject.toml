[tool.poetry]
name = "api-gateway"
version = "0.1.0"
description = "API 网关服务"
authors = ["Your Name <your.email@example.com>"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12.0,<3.13"
fastapi = "0.110.0"
uvicorn = "0.29.0"
httpx = "0.27.2"
jinja2 = "^3.1.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = "api.api:start_app" 