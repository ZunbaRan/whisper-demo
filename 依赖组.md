# 使用依赖组方案处理 crawl4ai 相关依赖

将 crawl4ai 及其相关依赖移至单独的组是一个很好的方案，可以避免与主项目的依赖冲突。下面是修改后的 `pyproject.toml` 文件：

```toml:pyproject.toml
[tool.poetry]
name = "whisper-deep-research"
version = "0.1.0"
description = "RSS 音频处理系统与深度思考分析"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9.7,<3.12.0"
# Whisper相关依赖
numpy = "1.23.5"
pandas = "2.2.2"
torch = { version = "2.5.1+cu121", source = "pytorch" }
whisperx = "3.3.1"
fastapi = "0.110.0"
uvicorn = "0.27.1"
pydantic = "2.6.3"  # 保持原始版本
requests = "^2.31.0"
httpx = "^0.27.0"
aiohttp = "^3.9.3"
aiofiles = "^23.2.1"  # 保持原始版本
humanize = "^4.9.0"
pyannote-audio = "^3.0.0"
beautifulsoup4 = "^4.9.3"
# 从这里移除 crawl4ai
selenium = "^4.18.1"
python-multipart = "^0.0.6"
jinja2 = "^3.1.2"
pyyaml = "^6.0.1"
pydub = "^0.25.1"

# Deep Research相关依赖
arkitect = "^0.1.4"
tavily-python = "^0.5.1"
volcengine-python-sdk = ">=1.0.123"
openpyxl = "^3.1.5"
openai = "^1.63.0"
tiktoken = "0.8.0"
gradio = "^5.17.1"
websockets = "^12.0.0"

# 创建单独的依赖组，包含crawl4ai及其相关依赖
[tool.poetry.group.web_crawl.dependencies]
crawl4ai = "^0.4.248"
pydantic = "^2.10.0"  # crawl4ai需要的版本
aiofiles = "^24.1.0"  # crawl4ai需要的版本
beautifulsoup4 = "^4.12.0"  # 如果需要更新版本
# 其他可能的crawl4ai依赖项

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
black = "^23.3.0"
isort = "^5.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.black]
line-length = 88
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 88

[tool.poetry.scripts]
start = "main:main"  # 启动主应用
dev = "uvicorn:run"  # 开发模式运行

[[tool.poetry.source]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu121"
priority = "explicit"
```

## 使用说明

### 1. 安装依赖

配置好 `pyproject.toml` 后，您可以根据需要选择安装方式：

```powershell
# 安装主依赖但不包括web_crawl组
poetry install --without web_crawl

# 安装所有依赖（包括web_crawl组）
poetry install
```

### 2. 代码中使用 crawl4ai

当您需要使用 `crawl4ai` 功能时，您有两种方式：

#### 方式1：如果在代码中直接使用

如果您的代码直接导入并使用 `crawl4ai`，您应该在运行前安装 `web_crawl` 组：

```powershell
poetry install  # 安装所有依赖，包括web_crawl组
```

然后在代码中正常导入并使用：

```python
import crawl4ai
# 使用 crawl4ai 的代码...
```

#### 方式2：条件导入

在代码中使用条件导入，只在 `crawl4ai` 可用时才使用它：

```python
try:
    import crawl4ai
    CRAWL4AI_AVAILABLE = True
except ImportError:
    CRAWL4AI_AVAILABLE = False

def fetch_web_content(url):
    if CRAWL4AI_AVAILABLE:
        # 使用 crawl4ai
        return crawl4ai.fetch(url)
    else:
        # 使用替代方法
        import requests
        from bs4 import BeautifulSoup
        response = requests.get(url)
        soup = BeautifulSoup(response.text, 'html.parser')
        # 处理并返回内容...
```

### 3. 修改现有代码

您需要在导入 `crawl4ai` 的代码中添加错误处理，或者确保在运行前安装了 `web_crawl` 组。

例如，您可以修改使用 `crawl4ai` 的服务类：

```python
class WebContentService:
    def __init__(self):
        try:
            import crawl4ai
            self.crawler = crawl4ai
            self.use_crawl4ai = True
        except ImportError:
            self.use_crawl4ai = False
            print("Warning: crawl4ai not available, using fallback method")
    
    def fetch_content(self, url):
        if self.use_crawl4ai:
            return self.crawler.fetch(url)
        else:
            # 实现替代方法
            # ...
```

## 优势与注意事项

### 优势

1. **避免依赖冲突**：主项目和 `crawl4ai` 可以使用各自需要的 `pydantic` 和 `aiofiles` 版本
2. **按需安装**：可以选择是否安装 `web_crawl` 组，减少不必要的依赖
3. **清晰的依赖分组**：提高项目的可维护性
4. **主项目稳定性**：避免为了兼容性而更新主项目依赖

### 注意事项

1. **代码适配**：需要修改代码以适应 `crawl4ai` 可能不可用的情况
2. **文档说明**：应在项目文档中说明依赖组和安装方式
3. **CI/CD 配置**：如果使用 CI/CD，可能需要调整配置以安装全部依赖

希望这个依赖组方案能帮助您解决依赖冲突问题！如果您需要对特定代码进行调整以适应这个方案，请告诉我，我会提供更具体的建议。
